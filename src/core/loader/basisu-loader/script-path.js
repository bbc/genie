/**
 * @copyright BBC 2023
 * @author BBC Children's D+E
 * @license Apache-2.0
 */
//TODO what happens when this is minified?
import { gmi } from "../../gmi/gmi.js";

export const SCRIPT_PATH = `${gmi.info.gameDir}lib/basisu`; // import.meta.url.split("/").slice(0,-1).join("/");
